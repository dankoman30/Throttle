# DEVELOPMENT/transmitter

Placeholder. Bench-test rig for the handle/trigger unit — deferred until the
receiver bench rig (`../receiver/`) proves out the approach (reusing the real
`src/handle/handle_firmware.c` state logic via the same
`#include "handle_firmware.c"` technique, driven by local trigger
pot/kill-switch/start-button/cruise-button input instead of real HAL reads).

Nothing here yet.
